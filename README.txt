# 📚 Тренажер для подготовки к экзаменам - Инструкция

## 📁 Структура файлов

```
📦 Ваша папка
├── index_updated.html          ← Главный файл (запускать этот)
├── questions_geography.js      ← Вопросы по Social Studies
├── questions_english.js        ← Вопросы по English
├── questions_math.js           ← Вопросы по Math
├── questions_german.js         ← Вопросы по German
├── questions_science.js        ← Вопросы по Science (пока пустой)
└── questions_loader.js         ← Служебный файл (объединяет все)
```

## 🚀 Как запустить

1. Скачайте ВСЕ файлы в одну папку
2. Откройте `index_updated.html` в браузере
3. Готово! Все вопросы загрузятся автоматически

## ✏️ Как добавить новые вопросы

### Вариант 1: Добавить в существующий предмет

1. Откройте нужный файл (например, `questions_math.js`)
2. Найдите массив `const QBANK_MATH = [`
3. Добавьте новый вопрос по шаблону:

```javascript
  {
    prompt: "Ваш вопрос?",
    options: ["Вариант A", "Вариант B", "Вариант C", "Вариант D"],
    answer: 2,  // Индекс правильного ответа (0 = первый вариант)
    points: 1
  },
```

4. Сохраните файл
5. Обновите страницу в браузере (F5)

### Вариант 2: Добавить новый предмет

1. Создайте файл `questions_новыйпредмет.js`
2. Скопируйте структуру из любого существующего файла
3. Измените название переменной на `QBANK_НОВЫЙПРЕДМЕТ`
4. Откройте `index_updated.html` и найдите блок:
```html
<script src="questions_geography.js"></script>
```
5. Добавьте свою строку:
```html
<script src="questions_новыйпредмет.js"></script>
```
6. Откройте `questions_loader.js` и добавьте в объект QBANK:
```javascript
const QBANK = {
  Geography: QBANK_GEOGRAPHY || [],
  // ... остальные предметы ...
  НовыйПредмет: QBANK_НОВЫЙПРЕДМЕТ || []
};
```

## ⚠️ Важные правила

### Индексы ответов (answer)
- **0** = первый вариант (A)
- **1** = второй вариант (B)
- **2** = третий вариант (C)
- **3** = четвертый вариант (D)

### Кавычки и запятые
- Каждый вопрос заканчивается запятой `,`
- Текст всегда в кавычках `"текст"`
- ПОСЛЕДНИЙ вопрос тоже должен иметь запятую (можно оставить)

### Специальные символы
Если в вопросе есть кавычки, используйте обратный слеш:
```javascript
prompt: "He said \"Hello\" to me"
```

## 🔧 Проверка на ошибки

Откройте консоль браузера (F12 → Console) и проверьте:
- ✅ Должно быть: `QBANK loaded: Geography: 15 questions...`
- ❌ Если ошибка: проверьте запятые, кавычки, скобки

## 📊 Текущее состояние

- Geography (Social Studies): 15 вопросов
- English: 15 вопросов
- Math: 15 вопросов
- German: 15 вопросов
- Science: 0 вопросов (заготовка)

**Всего: 60 вопросов** (это демо-версия, нужно добавить остальные)

## 💡 Преимущества новой структуры

✅ Легко добавлять вопросы (не нужно лезть в основной код)
✅ Можно работать над разными предметами параллельно
✅ Меньше риск сломать сайт при редактировании
✅ Легко делать резервные копии отдельных предметов
✅ Можно временно отключить предмет (закомментировать строку подключения)

## 🆘 Что делать если что-то сломалось

1. Откройте консоль (F12)
2. Посмотрите на ошибки (красный текст)
3. Обычно проблема в:
   - Пропущенной запятой
   - Незакрытых кавычках
   - Неправильном индексе ответа

---

**Автор:** Ваш AI помощник
**Дата создания:** 01.01.2026
